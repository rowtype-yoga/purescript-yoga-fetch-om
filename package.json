{
  "name": "purescript-yoga-fetch-om",
  "version": "0.4.0",
  "description": "Derive type-safe fetch clients from yoga HTTP API definitions",
  "type": "module",
  "scripts": {
    "build": "spago build",
    "test": "spago build && bunx vitest run",
    "clean": "rm -rf output .spago",
    "watch": "spago build --watch",
    "repl": "spago repl",
    "docs": "spago docs",
    "bundle": "spago bundle-app --main Complete.Example --to dist/index.js"
  },
  "keywords": [
    "purescript",
    "fetch",
    "http",
    "client",
    "api",
    "yoga",
    "type-safe"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^28.0.0",
    "purescript": "^0.15.15",
    "spago": "0.93.42",
    "vitest": "^2.1.8"
  },
  "dependencies": {}
}
